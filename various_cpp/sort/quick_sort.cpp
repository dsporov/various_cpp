#include <vector>


// todo: implement and test also selection (return k-th element)
int quick_select(std::vector<int>& a, int k) {
	throw "not implemented";
}

void quick_sort(std::vector<int> &a) {
	// todo - mistake w duplicate keys - putt al litems equal to partitioning element on one side
	// classic, median of 3, 3-way partitioning, median of 9?
}